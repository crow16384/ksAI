The study contains the following outputs. Use the registered tools
(`get_table_context`, `get_table_data`, `search_tables`, `compare_tables`,
`list_tables`, `get_study_index`) to retrieve the details of any output you
need before answering.

{{index}}
