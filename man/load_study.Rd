% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{load_study}
\alias{load_study}
\title{Load a Study from a ksTFL Meta Folder or a Saved \code{.ks} File}
\usage{
load_study(
	path,
	latest_only = TRUE,
	max_rows = ks_get_option("max_rows"),
	pattern = NULL,
	ignore_case = TRUE
)
}
\arguments{
\item{path}{Character scalar. Either a ksTFL meta folder (containing
\verb{_index.json} and the spec/data JSON files) or a \code{.ks} file produced by
\code{\link[=save_study]{save_study()}}.}

\item{latest_only}{Logical. When reading a meta folder, keep only the most
recent spec per document (via \code{is_latest}). Default \code{TRUE}.}

\item{max_rows}{Integer. Maximum data rows embedded per table context.
Defaults to \code{ks_get_option("max_rows")}.}

\item{pattern}{Optional regular expression. When set, keeps only outputs
whose id, type, or title matches the pattern.}

\item{ignore_case}{Logical. Passed to \code{\link[=grepl]{base::grepl()}} when matching
\code{pattern}. Default \code{TRUE}.}
}
\value{
A \link{ks_study} object.
}
\description{
Reads the metadata and data JSON artefacts produced by
\code{\link[ksTFL:save_report]{ksTFL::save_report()}} and compiles them into a \link{ks_study} registry of
\link{ks_context} objects. Alternatively, reloads a study previously written by
\code{\link[=save_study]{save_study()}}.
}
\examples{
\dontrun{
study <- load_study("path/to/outputs/meta")
study
study$tables[["14-3.01"]]
}

}
