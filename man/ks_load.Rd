% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{ks_load}
\alias{ks_load}
\title{Load Selected Outputs from a ksTFL Meta Folder or \code{.ks} File}
\usage{
ks_load(
  path,
  ids = NULL,
  latest_only = TRUE,
  max_rows = ks_get_option("max_rows")
)
}
\arguments{
\item{path}{Character scalar. ksTFL meta folder or \code{.ks} file.}

\item{ids}{Optional character vector of output ids to load. If \code{NULL}, all
available outputs are loaded.}

\item{latest_only}{Logical. When reading a meta folder, keep only reports
marked as latest if the index carries \code{is_latest}.}

\item{max_rows}{Integer. Maximum table rows embedded per context.}
}
\value{
A \link{ks_study} object containing only the selected outputs.
}
\description{
Loads only the requested output ids into a \link{ks_study} object.
}
\examples{
\dontrun{
study <- ks_load("path/to/outputs/meta", ids = c("14-3.01", "14-3.02"))
}

}
