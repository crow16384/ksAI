% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import.R
\name{ks_list_ids}
\alias{ks_list_ids}
\title{List Available Output IDs in a Meta Folder or \code{.ks} File}
\usage{
ks_list_ids(path, latest_only = TRUE)
}
\arguments{
\item{path}{Character scalar. ksTFL meta folder or \code{.ks} file.}

\item{latest_only}{Logical. When reading a meta folder, keep only reports
marked as latest if the index carries \code{is_latest}.}
}
\value{
Data frame with columns \code{id}, \code{type}, and \code{title}.
}
\description{
For a ksTFL meta folder, this scans spec metadata and returns available
output ids without loading table data. For a saved \code{.ks} file, ids are read
from the serialized study payload.
}
\examples{
\dontrun{
ids <- ks_list_ids("path/to/outputs/meta")
ids
}

}
