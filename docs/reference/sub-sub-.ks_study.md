# Look up an output by id across all types

Look up an output by id across all types

## Usage

``` r
# S3 method for class 'ks_study'
x[[i]]
```

## Arguments

- x:

  A `ks_study`.

- i:

  Character scalar output id.

## Value

The matching `ks_context`, or `NULL`.
