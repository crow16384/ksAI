# Load a Capsule Store from a `.ksc` File

Load a Capsule Store from a `.ksc` File

## Usage

``` r
load_capsules(path)
```

## Arguments

- path:

  Path to a `.ksc` file.

## Value

A `ks_capsule_store`.
