# Save a Capsule Store to a `.ksc` File

Save a Capsule Store to a `.ksc` File

## Usage

``` r
save_capsules(store, path)
```

## Arguments

- store:

  A `ks_capsule_store`.

- path:

  Output path (`.ksc` appended if missing).

## Value

The path written, invisibly.
