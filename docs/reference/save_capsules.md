# Save a Capsule Store to a `.ksc` File

Save a Capsule Store to a `.ksc` File

## Usage

``` r
save_capsules(store, path)
```

## Arguments

- store:

  A `ks_capsule_store`.

- path:

  Output path. If extension is missing, `.ksc` is appended.

## Value

Invisibly, written path.
